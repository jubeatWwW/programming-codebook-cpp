{
  "tasks": [
    {
      "type": "shell",
      "label": "g++ build active file",
      "command": "/usr/bin/g++",
      "args": [
        "-g",
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}"
      ],
      "options": {
        "cwd": "/usr/bin"
      }
    },
    {
      "label": "build-debug",
      "command": "make",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}/Elementary Data Structures/Array/"
      }
    }
  ],
  "version": "2.0.0"
}